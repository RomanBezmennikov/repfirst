Создайте два кортежа наполните их числовыми значениями
так чтобы было ~ 3-4  одинаковых числа входящих в каждый кортеж
Выполнить подсчет совпадающих чисел в обоих кортежах
и сформировать новый кортеж из этих чисел

для тех кому легко:
попробуйте реализовать итоговый кортеж без использования дополнительных переменных()
т.е. используя только 2 кортежа(создавать еще переменные под кортежи нельзя)